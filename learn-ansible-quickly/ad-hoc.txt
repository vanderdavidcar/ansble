 1026  ansible nodes -m shell "uptime"
 1027  ansible nodes -m shell -a "uptime"
 1030  ansible nodes -m shell -a "uptime"
 1039  ansible nodes -m shell -a "uptime"
 1049  ansible nodes -m shell -a "uptime"
 1051  ansible nodes -m shell -a "uptime" -u automation --ask-pass
 1054  ansible nodes -m shell -a "uptime"
 1123  ansible nodes -m shell -a "uptime"
 1139  ansible nodes -m shell -a "uptime"
 1140  sudo ansible nodes -m shell -a "lscpu | head -n 5"
 1141  sudo ansible nodes -m shell -a "whoami"
 1142  sudo ansible nodes -m shell -a "cat /etc/os-release"
 1146  sudo ansible nodes -m shell -a "lscpu | head -n 5"
 1147  sudo ansible nodes -m shell -a "yum install python3", "uptime", "toch /tmp/hello.txt"
 1148  sudo ansible nodes -m shell -a "yum install python3", "uptime", "touch /tmp/hello.txt"
 1149  sudo ansible nodes -m shell -a "yum install python3, uptime, touch /tmp/hello.txt"
 1159  sudo ansible nodes -m shell -a "uptime, touch /tmp/hello.txt"
 1160  sudo ansible nodes -m shell -a "uptime"
 1161  sudo ansible nodes -m shell -a "touch /tmp/hello.txt"
 1183  ansible nodes -m shell -a "ls /tmp
 1184  ansible nodes -m shell -a "ls /tmp"
 1185  ansible nodes -m shell -a "ls /tmp | grep foo"
 1186  ansible nodes -m shell -a "ls -lha /tmp | grep foo"
 1189  ansible nodes -m shell -a "which git"
 1190  ansible nodes -m shell -a "which tmux"
 1192  ansible nodes -m shell -a "which tmux"
 1193  ansible nodes -m shell -a "which git"
 1194  ansible nodes -m shell -a "ls /tmp"
 1195  ansible nodes -m shell -a "unzip /tmp/logs.tar.gz"
 1200  ansible nodes -m shell -a "which gunzip"
 1201  ansible nodes -m shell -a "which unzip"
 1203  ansible nodes -m shell -a "which unzip"
 1204  ansible nodes -m shell -a "ls /tmp"
 1205  ansible nodes -m shell -a "gunzip /tmp/logs.tar.gz"
 1206  ansible nodes -m shell -a "ls /tmp"
 1207  ansible nodes -m shell -a "cat /tmp/logs.tar"
 1243  ansible all -m shell -a "which git"
 1247  ansible all -m shell -a "which tmuz"
 1248  ansible all -m shell -a "which tmux"
 1251  ansible all -m shell -a "ls /etc/passwd"
 1252  ansible all -m shell -a "cat /etc/passwd"
 1253  ansible all -m shell -a "cat /etc/group"
 1257  ansible all -m shell -a "cat /etc/group"
 1260  ansible all -m shell -a "which ntpdate"
 1295  ansible all -m shell -a "which nmap"
 1296  ansible all -m shell -a "which tmux"
 1299  ansible all -m shell -a "ls -lha /tmp/ | grep bzip"
 1303  ansible all -m shell -a "which namp"
 1304  ansible all -m shell -a "which nmap"
 1305  ansible all -m shell -a "which ntpdate"
 1306  ansible all -m shell -a "which tmux"
 1308  ansible all -m shell -a "which tmux"
 1310  ansible all -m shell -a "which mariadb-server"
 1311  ansible all -m shell -a "which mariadb"
 1312  ansible all -m shell -a "which db"
 1313  ansible all -m shell -a "which maria"
 1314  ansible all -m shell -a "which maria*"
 1315  ansible all -m shell -a "which "
 1318  ansible all -m shell -a "which mariadb"
 1320  ansible all -m shell -a "which nmap"
 1322  ansible all -m shell -a "which nmap"
 1324  ansible all -m shell -a "which nmap"
 1325  ansible all -m shell -a "which tmux"
 1327  ansible all -m shell -a "which tmux"
 1328  ansible all -m shell -a "which tmux, nmap"
 1329  ansible all -m shell -a "which nmap"
 1330  ansible all -m shell -a "which nmap , tmux"
 1331  ansible all -m shell -a "which {nmap,tmux}"
 1332  ansible all -m shell -a "which {nmap,tmux,unzip,gunzip}"
 1334  ansible all -m shell -a "which {nmap,tmux,unzip,gunzip}"
 1336  ansible all -m shell -a "which {nmap,tmux,unzip,gunzip}"
 1741  ansible nodes -m shell -a "echo "Instances created via terraform" > /etc/motd"
 1742  ansible nodes -m shell -a "echo Instances created via terraform" > /etc/motd"
 1771  ansible nodes -m shell "cat /etc/motd"
 1772  ansible nodes -m shell -a "cat /etc/motd"
 1784  ansible nodes -m shell -a "cat /etc/motd"
 1858  ansible nodes -m shell -a "touch /etc/ansible/facts.d"
 1862  ansible nodes -m shell -a "mkdir /etc/ansible/facts.d"
 1865  ansible nodes -m shell -a "ls -lha /etc/ansible/facts.d"
 1866  ansible node01 -m shell -a "ls -lha /etc/ansible/"
 1870  ansible node01 -m shell -a "cat /etc/ansible/cool.fact"
 1871  ansible node01 -m shell -a "cat /etc/ansible/facts.d/cool.fact"
 1872  ansible node01 -m shell -a "sed -i 's/octopus/octupus/g' > /etc/ansible/facts.d/cool.fact"
 1873  ansible node01 -m shell -a "sed -i 's/octopus/octupus/g' /etc/ansible/facts.d/cool.fact"
 1874  ansible node01 -m shell -a "cat /etc/ansible/facts.d/cool.fact"
 1875  ansible node01 -m shell -a "ls /etc/ansible/facts.d/"
 1876  ansible node01 -m shell -a "cat /etc/ansible/facts.d/cool.fact"
 1886  ansible node01 -m shell -a "cat /etc/ansible/facts.d/cool.fact"
 1896  ansible nodes -m shell -a "free -h"
 1901  ansible node01 -m shell  -a "setup"
 1905  ansible node01 -m shell -a "ifconfig | grep inet"
 1906  ansible node01 -m shell -setup"
 1918  ansible node01 -m shell -a "free -h"
 1995  ansible node04 -m shell -a "cat /etc/passwd | grep brad"
 1998  ansible node04 -m shell -a "tail -3 /etc/passwd"
 2004  ansible node04 -m shell -a "tail -3 /etc/passwd"
 2005  ansible node04 -m shell -a "tail /etc/passwd"
 2018  history | grep shell
 2019  history | grep shell|command 
 2020  history | grep shell | grep command 
 2021  history | grep shell
 2022  history | grep shell > ad-hoc.txt
